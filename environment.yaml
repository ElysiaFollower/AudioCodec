name: audiocodec
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip>=24
  - ffmpeg
  - matplotlib
  - numpy
  - setuptools>=68
  - wheel
  - pip:
      - tensorboard>=2.16,<3
      - torch>=2.2,<3
      - torchaudio>=2.2,<3
      - -e .
